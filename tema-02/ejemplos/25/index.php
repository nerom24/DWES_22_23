<?php

    /* 
        Función isnull(), isset(), empty()
    */

    // Caso 1: Variable no definida

    // var_dump(is_null($var));
    // var_dump(isset($var));
    // var_dump(empty($var));

    // Caso 2: Variable definida null

    // $var = 1;
    // var_dump(isset($var));

    // Caso 3. empty()

    $var = "3";
    var_dump(empty($var));






?>